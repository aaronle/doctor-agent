# Doctor Agent 界面文件

- `current/`：当前已确认的产品界面基线，由最早 HTML 派生，只包含已确认的小范围调整。
- `prototype/`：2026-08-28 形成的 UI/UX 重设计探索稿，仅用于历史追溯，不属于一期需求和验收基线。

生产实现、测试截图和 UI 对照必须以 `current/AI-HIS医生智能体一期.html` 为当前基线，并与 `references/ui-demo/AI-HIS门诊模块V4.3.html` 的原始结构和交互进行差异核对。
